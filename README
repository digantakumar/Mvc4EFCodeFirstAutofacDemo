The following has been achieved in this project,

1. The solution is divided into client, domain and data access projects.
2. Entity Framework code first to generate the database from the entities from the Domain project.
3. IoC (Autofac) convention is use to register all data access repository code with the client.